#include<bits/stdc++.h>
using namespace std;
void reverse(char* c,int c)
{
stack<char> s;
for(int i=0;i<n-1;i++)
{
s.push(c[i]);
}
for(int i=0;i<n-1;i++)
{
c[i]=s.top();
s.pop();
}
int main()
{
char c[51];
printf("enter a string : ");
gets(c);
reverse(c,strlen(c));
printf("output = %s",c);
}
